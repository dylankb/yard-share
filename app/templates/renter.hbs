<div class="container renter-profile">
  <div class="col-sm-3">
      <img class="renter" src={{model.renter.image}} alt={{model.renter.username}} />
    </div>
    <div class="col-sm-9">
      <h1>{{model.renter.username}}</h1>
      <p><strong>About {{model.renter.username}}:</strong> <br>{{model.renter.description}}</p>

    <!-- {{renter-details renter=model}} -->
  </div>
  <div class="col-sm-12">
    {{renter-update renter=model updateRenter="updateRenter"}}
  </div>

  <div class="col-sm-6">
    <h2>Bookings</h2>
    {{#each model.bookings as |booking|}}
    {{booking-tile booking=booking}}
    {{/each}}
  </div>
  <div class="col-sm-6">
    <h2>User Reviews</h2>
    <h5>Average Rating: {{rating-stars model.ratingAverage}}</h5>
    {{review-new renter=model.renter saveReview="saveReview"}}
    {{#each model.renter.reviews as |review|}}
      {{review-tile-owner review=review}}
    {{/each}}
  </div>
</div>
