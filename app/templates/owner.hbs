{{owner-details owner=model}}
{{listing-new owner=model saveListing="saveListing"}}

<div class="owner-listing-tiles">
  {{#each model.listings as |listing|}}
    {{listing-tile listing=listing}}
  {{/each}}
</div>
