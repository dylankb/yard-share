{{#if updateOwnerForm}}
<h2>Update my Profile:</h2>
<form>
  <div class="form-group">
    <label for="owner-description">Description</label>
    {{input value=ownerDescription id="owner-description"}}
    <label for="owner-image">Image URL</label>
    {{input value=ownerImage id="owner-image"}}
  </div>
  <button class="btn btn-default"{{action 'updateOwner' owner}}>Save</button>
</form>
{{else}}
  <button class="btn btn-default"{{action 'updateOwnerFormShow'}}>Update my Profile</button>
{{/if}}
